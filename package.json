{
  "name": "peace-net",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "ui:add": "pnpm --filter @peace-net/ui ui:add",
    "db:start": "npx supabase start",
    "db:stop": "npx supabase stop",
    "prepare": "husky install",
    "preinstall": "npx -y only-allow pnpm"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.2.7",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.6.5",
    "turbo": "^2.0.11",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  }
}
